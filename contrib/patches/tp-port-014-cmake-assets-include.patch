--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -117,6 +117,8 @@ if(TP_BUILD_GAME)
     add_library(tp_game_code STATIC ${TP_GAME_SOURCES})
     target_include_directories(tp_game_code PUBLIC
         ${CMAKE_SOURCE_DIR}/include
+        # For #include "assets/..." (headers generated by zeldaret/tp build; see docs/native-port-ubuntu.md)
+        ${CMAKE_SOURCE_DIR}
         ${CMAKE_SOURCE_DIR}/extern/aurora/include
     )
     target_compile_definitions(tp_game_code PRIVATE
